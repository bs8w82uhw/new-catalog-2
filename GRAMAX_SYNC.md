# Gramax Sync

This repo is connected to Gramax and used as a docs source.

## GitHub Secret

Add repository secret:

- `GRAMAX_SYNC_WEBHOOK_URL`

## Auto Sync

Workflow `.github/workflows/docs-sync-gramax.yml` sends changed files to the webhook on every push to `master`.
